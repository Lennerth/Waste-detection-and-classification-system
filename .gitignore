# Python cache
__pycache__/
*.py[cod]

# SAM 2 model checkpoints (large; auto-downloaded by ensure_checkpoint)
*.pt

# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fdb_latexmk
*.fls
report.pdf

# Report source and its Overleaf figure copies are kept local, not tracked
report.tex
figures/

# Reference notebook kept local, not tracked in the repository
automatic_mask_generator_example.ipynb
